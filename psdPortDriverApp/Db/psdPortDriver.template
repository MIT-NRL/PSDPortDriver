###################################################################
#  These records control run/stop                                 #
###################################################################
record(bo, "$(P)$(R)Acquire")
{
    field(DTYP, "asynInt32")
    field(OUT,  "@asyn($(PORT),$(ADDR),$(TIMEOUT))PSD_ACQUIRE")
    field(ZNAM, "Done")
    field(ONAM, "Acquire")
}

record(bi, "$(P)$(R)Run_RBV")
{
    field(DTYP, "asynInt32")
    field(INP,  "@asyn($(PORT),$(ADDR),$(TIMEOUT))PSD_ACQUIRE")
    field(ZNAM, "Done")
    field(ZSV,  "NO_ALARM")
    field(ONAM, "Acquiring")
    field(OSV,  "MINOR")
    field(SCAN, "I/O Intr")
}

###################################################################
#  This record is the histogram                                   #
###################################################################
record(waveform, "$(P)$(R)Histogram_RBV")
{
    field(DTYP, "asynInt32ArrayIn")
    field(INP,  "@asyn($(PORT),$(ADDR),$(TIMEOUT))PSD_HISTOGRAM")
    field(FTVL, "LONG")
    field(NELM, "$(NBINS)")
    field(SCAN, "I/O Intr")
}
